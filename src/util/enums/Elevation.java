package util.enums;

/**
 * The elevation to be used for calculations.  {@code ACTUAL} uses the current elevation of the location for certain
 * elements.
 */
public enum Elevation
{
    SEA_LEVEL,
    ACTUAL
}
